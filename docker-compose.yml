version: "3"
services:
  apps:
    build:
      context: ./apps
      dockerfile: "Dockerfile"
    privileged: true
    container_name: "apps"
    #command: sh -c 'npm install && npm run dev'
    #volumes:
    #  - ./naviterm:/home/naviterm/
    ports:
      - "4000:3000"
    tty: true
